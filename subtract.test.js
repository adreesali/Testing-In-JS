const subtract = require('./subtract')

test('properly subtracting two numbers', () => {
    expect(
        subtract(1,2)
    ).toBe(-1)
} )


// const subtract = require('./subtract')

// test('property subtracting two numbers ', () => {
//     expect(subtract(1,2).toBe(-1))
// })